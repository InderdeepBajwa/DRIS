import { VisualizationModel } from './visualization.model';

describe('Visualization.Model', () => {
  it('should create an instance', () => {
    expect(new VisualizationModel()).toBeTruthy();
  });
});
